---
import BaseLayout from "../../layouts/BaseLayout.astro";
import HeroSection from "../../components/HeroSection.astro";
import Breadcrumb from "../../components/Breadcrumb.astro";
import ContentSectionHeader from "../../components/ContentSectionHeader.astro";

const siteKey = import.meta.env.PUBLIC_RECAPTCHA_SITE_KEY;

// パンくずリスト
const breadcrumbItems = [
  { href: "/", label: "ホーム" },
  { label: "お問い合わせ", current: true }
];
---

<BaseLayout title="お問い合わせ" description="むなかた子ども大学へのお問い合わせ先をご案内します。">
  <main>
    <!-- ヒーローセクション -->
    <HeroSection 
      title="お問い合わせ"
      subtitle="Contact"
      imageSrc="/images/backgrounds/img_bg-hero_contact-01.webp"
      imageAlt="お問い合わせ"
    />

    <!-- パンくずリスト -->
    <Breadcrumb items={breadcrumbItems} />

    <!-- お問い合わせ情報セクション -->
    <section class="contact-info-section">
      <div class="contact-content__container">
        <div class="contact-info">
          <h3 class="contact-info__title">むなかた子ども大学　事務局</h3>
          
          <dl class="contact-info__list">
            <dt>部署名</dt>
            <dd>
              宗像市教育委員会<br>
              （教育部 教育総務課 地域教育連携室 グローバル人材育成係）
            </dd>
            
            <dt>窓口</dt>
            <dd>
              〒811-3436　福岡県宗像市東郷1丁目1-1<br>
              宗像市役所　本館3階
            </dd>
          </dl>
        </div>

        <!-- ナビゲーションメニュー -->
        <div class="page-nav">
          <div class="page-nav__container">
            <nav class="page-nav__menu">
              <a href="#phone-fax" class="page-nav__link">
                <div class="page-nav__arrow"></div>
                <span class="page-nav__text">電話・FAXでの<br class="u-br-sm">お問い合わせ</span>
              </a>
              <a href="#contact-form" class="page-nav__link">
                <div class="page-nav__arrow"></div>
                <span class="page-nav__text">メールでの<br class="u-br-sm">お問い合わせ</span>
              </a>
            </nav>
          </div>
        </div>
      </div>
    </section>

    <!-- 電話・FAXセクション -->
    <section id="phone-fax" class="contact-phone-section">
      <div class="contact-content__container">
        <div class="contact-phone">
          <ContentSectionHeader 
            englishTitle=""
            title="電話・FAX"
            class="fade-up"
          />
          
          <dl class="contact-phone__list">
            <dt>電話番号</dt>
            <dd><a href="tel:0940-36-1169" class="phone-link">0940-36-1169</a></dd>
            
            <dt>FAX番号</dt>
            <dd><span>0940-37-1525</span></dd>
          </dl>
        </div>
      </div>
    </section>

    <!-- お問い合わせフォームセクション -->
    <section id="contact-form" class="contact-form-section">
      <div class="contact-content__container">
        <div class="contact-form">
          <ContentSectionHeader 
            englishTitle=""
            title="お問い合わせフォーム"
            class="fade-up"
          />
          
          <div class="contact-form__intro">
            <p>むなかた子ども大学へのお問い合わせありがとうございます。<br>
            講座参加や協力のご相談、資料請求などについて、下記フォームに必要事項をご入力のうえ送信してください。<br>
            担当者より折り返しご連絡差し上げます。</p>
          </div>
          
          <!-- お問い合わせフォーム -->
          <form class="contact-form__form" id="contactForm" method="POST" 
                action="https://script.google.com/macros/s/AKfycbyTiLfUCQZyOZ77L-OHZaKUic3uQTYD-Yd81eheS6u7OD5yq6_cdvC8p5_ItVFdPJ8/exec"
                target="hidden_iframe">
            
            <!-- reCAPTCHA token（隠しフィールド） -->
            <input type="hidden" id="g-recaptcha-response" name="g-recaptcha-response">
            
            <!-- お問い合わせ種別 -->
            <div class="form-field">
              <label for="inquiry-type" class="form-field__label">
                お問い合わせ種別<span class="form-field__required">必須</span>
              </label>
              <select id="inquiry-type" name="inquiryType" class="form-field__select" required>
                <option value="">選択してください</option>
                <option value="講座について">講座について</option>
                <option value="遅刻・欠席の連絡">遅刻・欠席の連絡</option>
                <option value="取材について">取材について</option>
                <option value="その他">その他</option>
              </select>
            </div>

            <!-- お名前 -->
            <div class="form-field">
              <label for="name" class="form-field__label">
                お名前<span class="form-field__required">必須</span>
              </label>
              <input type="text" id="name" name="name" class="form-field__input" required>
            </div>

            <!-- お名前(カタカナ) -->
            <div class="form-field">
              <label for="name-kana" class="form-field__label">
                お名前（カタカナ）<span class="form-field__required">必須</span>
              </label>
              <input type="text" id="name-kana" name="nameKana" class="form-field__input" required>
            </div>

            <!-- 学校名または法人名 -->
            <div class="form-field">
              <label for="organization" class="form-field__label">
                学校名または法人名
              </label>
              <input type="text" id="organization" name="organization" class="form-field__input">
            </div>

            <!-- メールアドレス -->
            <div class="form-field">
              <label for="email" class="form-field__label">
                メールアドレス<span class="form-field__required">必須</span>
              </label>
              <input type="email" id="email" name="email" class="form-field__input" required>
            </div>

            <!-- 電話番号 -->
            <div class="form-field">
              <label for="phone" class="form-field__label">
                電話番号<span class="form-field__required">必須</span>
              </label>
              <input type="tel" id="phone" name="phone" class="form-field__input" required>
            </div>

            <!-- お問い合わせ内容 -->
            <div class="form-field">
              <label for="message" class="form-field__label">
                お問い合わせ内容
              </label>
              <textarea id="message" name="message" class="form-field__textarea" rows="8"></textarea>
            </div>

            <!-- プライバシーポリシー同意 -->
            <div class="form-field form-field--checkbox">
              <label class="form-field__checkbox-wrapper">
                <input type="checkbox" id="privacy-agreement" name="privacyAgreement" class="form-field__checkbox" required>
                <span class="form-field__checkbox-text">
                  <a href="/privacy-policy" target="_blank" class="form-field__privacy-link">プライバシーポリシー</a>をご確認の上、よろしければ「送信する」をクリックしてください。
                </span>
              </label>
            </div>

            <!-- 送信ボタン -->
            <div class="form-field form-field--submit">
              <button type="submit" id="submit-button" class="form-field__submit" disabled>
                送信する
              </button>
            </div>

          </form>
          
          <!-- 隠しiframe（フォーム送信用） -->
          <iframe name="hidden_iframe" id="hidden_iframe" style="display:none;"></iframe>
        </div>
      </div>
    </section>

  </main>

  <!-- reCAPTCHA v3 スクリプト -->
  <script src={`https://www.google.com/recaptcha/api.js?render=${import.meta.env.PUBLIC_RECAPTCHA_SITE_KEY}`}></script>

  <!-- カスタムモーダル -->
  <div id="modal-overlay" class="modal-overlay">
    <div class="modal-container">
      <div class="modal-content">
        <div class="modal-icon">
          <div class="modal-icon__success">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M9 12l2 2 4-4"></path>
              <circle cx="12" cy="12" r="10"></circle>
            </svg>
          </div>
          <div class="modal-icon__error">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="15" y1="9" x2="9" y2="15"></line>
              <line x1="9" y1="9" x2="15" y2="15"></line>
            </svg>
          </div>
        </div>
        <h3 class="modal-title" id="modal-title">送信完了</h3>
        <p class="modal-message" id="modal-message">
          お問い合わせありがとうございました。<br>
          内容を確認後、担当者より折り返しご連絡いたします。
        </p>
        <button class="modal-button" id="modal-close-btn">
          閉じる
        </button>
      </div>
    </div>
  </div>
</BaseLayout>

<script define:vars={{ siteKey }} is:inline>
// チェックボックスの状態に応じて送信ボタンの有効/無効を切り替え
document.addEventListener('DOMContentLoaded', function() {
  const privacyCheckbox = document.getElementById('privacy-agreement');
  const submitButton = document.getElementById('submit-button');
  const contactForm = document.getElementById('contactForm');

  if (privacyCheckbox && submitButton) {
    privacyCheckbox.addEventListener('change', function() {
      submitButton.disabled = !this.checked;
    });
  }

  // モーダル表示関数
  function showModal(type, title, message) {
    const modalOverlay = document.getElementById('modal-overlay');
    const modalTitle = document.getElementById('modal-title');
    const modalMessage = document.getElementById('modal-message');
    const modalContainer = modalOverlay?.querySelector('.modal-container');
    
    if (modalOverlay && modalTitle && modalMessage && modalContainer) {
      modalTitle.textContent = title;
      modalMessage.innerHTML = message;
      
      // モーダルのタイプに応じてクラスを設定
      modalContainer.className = `modal-container modal-container--${type}`;
      
      // モーダルを表示
      modalOverlay.classList.add('modal-overlay--show');
      document.body.style.overflow = 'hidden'; // スクロール無効化
    }
  }
  
  // モーダルを閉じる関数
  function closeModal() {
    const modalOverlay = document.getElementById('modal-overlay');
    if (modalOverlay) {
      modalOverlay.classList.remove('modal-overlay--show');
      document.body.style.overflow = ''; // スクロール復活
    }
  }
  
  // モーダル閉じるボタンのイベントリスナー
  const modalCloseBtn = document.getElementById('modal-close-btn');
  if (modalCloseBtn) {
    modalCloseBtn.addEventListener('click', closeModal);
  }
  
  // モーダルオーバーレイクリックで閉じる
  const modalOverlay = document.getElementById('modal-overlay');
  if (modalOverlay) {
    modalOverlay.addEventListener('click', function(e) {
      if (e.target === modalOverlay) {
        closeModal();
      }
    });
  }

  // フォーム送信処理（reCAPTCHA v3対応）
  if (contactForm) {
    contactForm.addEventListener('submit', function(e) {
      e.preventDefault();
      
      // 送信ボタンを無効化
      submitButton.disabled = true;
      submitButton.textContent = '送信中...';
      
      // reCAPTCHA v3 トークン取得
      window.grecaptcha.ready(function() {
        window.grecaptcha.execute(siteKey, {action: 'contact_form'}).then(function(token) {
          // 隠しフィールドにトークンを設定
          const tokenField = document.getElementById('g-recaptcha-response');
          if (tokenField) {
            tokenField.value = token;
          }
          
          // フォーム送信
          const formData = new FormData(contactForm);
          fetch(contactForm.action, {
            method: 'POST',
            body: formData
          }).then(response => {
            if (response.ok) {
              showModal('success', '送信完了', 'お問い合わせありがとうございました。<br>内容を確認後、担当者より折り返しご連絡いたします。');
              contactForm.reset();
              privacyCheckbox.checked = false;
            } else {
              throw new Error('送信に失敗しました');
            }
          }).catch(error => {
            console.error('送信エラー:', error);
            showModal('error', '送信エラー', '送信に失敗しました。<br>しばらく時間をおいて再度お試しください。');
          }).finally(() => {
            submitButton.disabled = true;
            submitButton.textContent = '送信する';
          });
        });
      });
    });
  }

  // スムーススクロール
  const navLinks = document.querySelectorAll('.page-nav__link');
  
  navLinks.forEach(link => {
    link.addEventListener('click', function(e) {
      e.preventDefault();
      
      const target = e.currentTarget;
      const href = target.getAttribute('href');
      const targetId = href?.substring(1);
      const targetElement = targetId ? document.getElementById(targetId) : null;
      
      if (targetElement) {
        targetElement.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
});
</script>

<style lang="scss">
  @import "../../styles/index";

  .contact-info-section {
    padding: rem(40) 0 0;
    
    @include breakpoint-up(lg) {
      padding: rem(60) 0 0;
    }
  }

  .contact-phone-section {
    padding: rem(40) 0 0;
    background: linear-gradient(to bottom, $background-primary 0%, $background-tertiary 100%);
    
    @include breakpoint-up(lg) {
      padding: rem(60) 0 0;
    }
  }

  .contact-form-section {
    padding: rem(40) 0;
    background: linear-gradient(to bottom, $background-tertiary 0%, $background-tertiary 80%, $background-secondary 100%);
    
    @include breakpoint-up(lg) {
      padding: rem(60) 0;
    }
  }

  .contact-form-section {
    padding-bottom: rem(120);
    
    @include breakpoint-up(xl) {
      padding-bottom: rem(160);
    }
  }

  .contact-content__container {
    margin: 0 auto;
    padding: 0 5%;
    overflow: hidden;
    @include breakpoint-up(xl) {
      padding: 0 rem(50);
    }
  }

  // お問い合わせ情報
  .contact-info {
    max-width: rem(800);
    margin: 0 auto;
    padding: rem(40) rem(20) rem(30);
    
    @include breakpoint-up(lg) {
      padding: rem(60) rem(40) rem(20);
    }
  }

  .contact-info__title {
    @include font-ja-m(semibold);
    color: $text-primary;
    text-align: center;
    margin: 0 0 rem(40) 0;
    
    @include breakpoint-up(lg) {
      @include font-ja-l(semibold);
      margin-bottom: rem(50);
    }
  }

  .contact-info__list {
    margin: 0 auto;
    padding: 0;
    width: fit-content;

    @include breakpoint-up(sm) {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: rem(8) rem(20);
      align-items: baseline;
      
      @include breakpoint-up(lg) {
        gap: rem(12) rem(30);
      }
    }

    dt {
      @include font-ja-s;
      color: $text-primary;
      margin: 0 0 rem(8) 0;
      
      @include breakpoint-up(sm) {
        margin: 0;
        justify-self: start;
      }

      &:not(:first-child) {
        margin-top: rem(32);
        
        @include breakpoint-up(sm) {
          margin-top: rem(24);
        }
        
        @include breakpoint-up(lg) {
          margin-top: rem(28);
        }
      }
    }

    dd {
      @include font-ja-s;
      color: $text-primary;
      margin: 0 0 0 rem(20);
      line-height: 1.8;
      
      @include breakpoint-up(sm) {
        margin: 0;
      }
    }
  }

  // 電話・FAX情報
  .contact-phone {
    
  }

  .contact-phone__list {
    margin: rem(40) auto 0;
    padding: 0;
    width: fit-content;
    text-align: center;

    @include breakpoint-up(sm) {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: rem(8) rem(20);
      align-items: center;
      text-align: left;
      
      @include breakpoint-up(lg) {
        gap: rem(12) rem(30);
      }
    }

    dt {
      @include font-ja-m;
      color: $text-primary;
      
      @include breakpoint-up(sm) {
        justify-self: start;
      }

      &:not(:first-child) {
        margin-top: rem(20);
        
        @include breakpoint-up(sm) {
          margin-top: 0;
        }
      }
    }

    dd {
      margin: 0 0 0 rem(20);
      line-height: 1.8;
      
      @include breakpoint-up(sm) {
        margin: 0;
      }

      a.phone-link {
        @include font-ja-l(semibold);
        color: $brand-turquoise;
        text-decoration: none;
        
        @include breakpoint-up(lg) {
          pointer-events: none;
        }
        
        &:hover {
          text-decoration: underline;
          
          @include breakpoint-up(lg) {
            text-decoration: none;
          }
        }
      }

      span {
        @include font-ja-l(semibold);
        color: $brand-turquoise;
      }
    }
  }

  // お問い合わせフォーム
  .contact-form {
  }

  .contact-form__intro {
    margin: 0 auto;
    padding: 0 5%;
    @include breakpoint-up(xl) {
      padding: 0 rem(50);
    }
    
    p {
      @include font-ja-s;
      color: $text-primary;
      line-height: 1.8;
      margin: 0;
      max-width: rem(1200);

      &:not(:last-child) {
        margin-bottom: rem(16);
      }
    }
  }

  // フォームスタイリング
  .contact-form__form {
    max-width: rem(900);
    margin: rem(40) auto 0;
    padding: 0 5%;
    
    @include breakpoint-up(lg) {
      padding: 0
    }
  }

  .form-field {
    margin-bottom: rem(32);

    @include breakpoint-up(lg) {
      display: flex;
      align-items: flex-start;
      gap: rem(24);
      margin-bottom: rem(40);
    }

    &--checkbox {
      margin-bottom: rem(24);
      padding-top: rem(24);
      display: flex;
      justify-content: center;
      
      @include breakpoint-up(lg) {
        display: flex;
        justify-content: center;
      }
    }

    &--submit {
      margin-bottom: 0;
      text-align: center;
      
      @include breakpoint-up(lg) {
        display: block;
      }
    }
  }

  .form-field__label {
    @include font-ja-s(semibold);
    color: $text-primary;
    display: block;
    margin-bottom: rem(8);
    
    @include breakpoint-up(lg) {
      flex-shrink: 0;
      width: rem(220);
      margin-bottom: 0;
      padding-top: rem(12);
    }
    @include breakpoint-up(lg) {
      width: rem(240);
    }
  }

  .form-field__required {
    @include font-ja-2s(semibold);
    color: $feedback-error;
    border: 1px solid $feedback-error;
    padding: rem(2) rem(6);
    border-radius: $radius-s;
    margin-left: rem(8);
  }

  .form-field__input,
  .form-field__select {
    width: 100%;
    padding: rem(10) rem(12);
    border: 1px solid $border-secondary;
    border-radius: $radius-s;
    @include font-ja-s;
    color: $text-primary;
    background: $background-primary;
    transition: border-color 0.3s ease;

    &:focus {
      outline: none;
      border-color: $brand-turquoise;
    }

    @include breakpoint-up(lg) {
      flex: 1;
      padding: rem(12) rem(16);
    }
  }

  .form-field__select {
    padding-right: rem(32);
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpath d='M6 9l6 6 6-6'/%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right rem(10) center;
    background-size: rem(20);
    appearance: none;

    @include breakpoint-up(lg) {
      background-position: right rem(10) center;
    }
  }

  .form-field__textarea {
    width: 100%;
    padding: rem(10) rem(12);
    border: 1px solid $border-secondary;
    border-radius: $radius-s;
    @include font-ja-s;
    color: $text-primary;
    background: $background-primary;
    resize: vertical;
    min-height: rem(120);
    transition: border-color 0.3s ease;

    &:focus {
      outline: none;
      border-color: $brand-turquoise;
    }

    @include breakpoint-up(lg) {
      flex: 1;
      padding: rem(12) rem(16);
      min-height: rem(160);
    }
  }

  .form-field__checkbox-wrapper {
    display: flex;
    align-items: flex-start;
    gap: rem(12);
    cursor: pointer;
  }

  .form-field__checkbox {
    flex-shrink: 0;
    width: rem(20);
    height: rem(20);
    margin: rem(2) 0 0 0;
    accent-color: $brand-primary;
  }

  .form-field__checkbox-text {
    @include font-ja-s;
    color: $text-primary;
    line-height: 1.6;
    flex: 1;
  }

  .form-field__privacy-link {
    color: $brand-turquoise;
    text-decoration: underline;
    font-weight: 600;

    &:hover {
      color: $brand-turquoise;
      text-decoration: underline;
    }
  }

  .form-field__submit {
    @include font-ja-s(semibold);
    color: $text-white;
    background: $brand-primary;
    border: none;
    padding: rem(24) rem(48);
    border-radius: rem(8);
    cursor: pointer;
    transition: all 0.3s ease;
    min-width: rem(200);

    &:hover:not(:disabled) {
      background: darken($brand-primary, 10%);
      transform: translateY(rem(-2));
    }

    &:disabled {
      background: $background-dark-gray;
      cursor: not-allowed;
      transform: none;
    }

    @include breakpoint-up(lg) {
      padding: rem(30) rem(60);
      min-width: rem(240);
    }
  }

  // ページナビゲーション
  .page-nav {
    padding: rem(20) 0 0;
    background: $background-primary;

    &__container {
      margin: 0 auto;
      padding: 0 5%;
      @include breakpoint-up(xl) {
        padding: 0 rem(50);
      }
    }

    &__menu {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0;

      @include breakpoint-up(sm) {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
      }
    }

    &__link {
      display: flex;
      align-items: center;
      gap: rem(5);
      color: $text-primary;
      text-decoration: none;
      transition: color 0.3s ease;
      padding: rem(6) rem(10);

      @include breakpoint-up(sm) {
        gap: rem(12);
        padding: rem(6) rem(20);
        border-right: 1px solid $border-secondary;
      }

      @include breakpoint-up(xl) {
        padding: rem(6) rem(30);
      }

      &:first-child {
        @include breakpoint-up(sm) {
          padding-left: 0;
        }
      }

      &:last-child {
        border-right: none;
      }

      &:hover {
        color: $brand-secondary;
        text-decoration: none;

        .page-nav__arrow {
          transform: translateY(rem(6)) rotate(45deg);
        }
      }
    }

    &__text {
      @include font-ja-s(semibold);
    }

    &__arrow {
      width: rem(9);
      height: rem(9);
      border-right: 2px solid currentColor;
      border-bottom: 2px solid currentColor;
      transform: rotate(45deg);
      transition: transform 0.3s ease;
      flex-shrink: 0;
      margin-right: rem(3);
    }
  }

  // カスタムモーダル
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    backdrop-filter: blur(4px);
    padding: rem(24) rem(20);

    @include breakpoint-up(sm) {
      padding: rem(40) rem(32);
    }

    @include breakpoint-up(lg) {
      padding: rem(60);
    }

    &--show {
      opacity: 1;
      visibility: visible;
    }
  }

  .modal-container {
    background: $background-primary;
    border-radius: rem(16);
    box-shadow: 0 rem(20) rem(60) rgba(0, 0, 0, 0.3);
    width: 100%;
    max-width: rem(480);
    transform: translateY(rem(30));
    transition: transform 0.3s ease;
    position: relative;

    .modal-overlay--show & {
      transform: translateY(0);
    }

    // 成功時のスタイル
    &--success {
      .modal-icon__success {
        display: block;
      }
      
      .modal-icon__error {
        display: none;
      }
    }

    // エラー時のスタイル
    &--error {
      .modal-icon__success {
        display: none;
      }
      
      .modal-icon__error {
        display: block;
      }
    }
  }

  .modal-content {
    padding: rem(40) rem(32) rem(32);
    text-align: center;

    @include breakpoint-up(lg) {
      padding: rem(50) rem(40) rem(40);
    }
  }

  .modal-icon {
    width: rem(60);
    height: rem(60);
    margin: 0 auto rem(24);
    
    @include breakpoint-up(lg) {
      width: rem(80);
      height: rem(80);
      margin-bottom: rem(32);
    }

    svg {
      width: 100%;
      height: 100%;
    }
  }

  .modal-icon__success {
    display: none;
    color: $brand-secondary;
    background: lighten($brand-secondary, 45%);
    border-radius: 50%;
    padding: rem(12);
    
    @include breakpoint-up(lg) {
      padding: rem(16);
    }
  }

  .modal-icon__error {
    display: none;
    color: $feedback-error;
    background: lighten($feedback-error, 40%);
    border-radius: 50%;
    padding: rem(12);
    
    @include breakpoint-up(lg) {
      padding: rem(16);
    }
  }

  .modal-title {
    @include font-ja-l(semibold);
    color: $text-primary;
    margin: 0 0 rem(16) 0;
    
    @include breakpoint-up(lg) {
      @include font-ja-l(semibold);
      margin-bottom: rem(20);
    }
  }

  .modal-message {
    @include font-ja-s;
    color: $text-primary;
    line-height: 1.7;
    margin: 0 0 rem(32) 0;
    
    @include breakpoint-up(lg) {
      @include font-ja-s;
      margin-bottom: rem(40);
    }
  }

  .modal-button {
    @include font-ja-s(semibold);
    background: $brand-primary;
    color: $text-white;
    border: none;
    border-radius: rem(8);
    padding: rem(16) rem(40);
    cursor: pointer;
    transition: all 0.3s ease;
    min-width: rem(120);

    &:hover {
      background: darken($brand-primary, 10%);
      transform: translateY(rem(-2));
    }

    &:active {
      transform: translateY(0);
    }

    @include breakpoint-up(lg) {
      padding: rem(18) rem(48);
      min-width: rem(140);
    }
  }


</style>